include header

.container
    h1 Операція з товаром
    form(method="POST", action="/inventory")
        input(type="hidden", name="productId", value=product._id)

        .form-group
            label(for="operationType") Тип операції
            select(name="operationType", required)
                option(value="received") Надходження
                option(value="removed") Зняття зі складу (продаж)
                option(value="serviced") Обслуговування
                option(value="moved") Переміщення
                option(value="written_off") Списання

        .form-group
            label(for="quantity") Кількість
            input(type="number", name="quantity", min="1", required)

        .form-group
            label(for="note") Примітка
            textarea(name="note")

        .form-group
            label(for="fromLocation") Звідки (для переміщення)
            input(type="text", name="fromLocation", value=product.location)

        .form-group
            label(for="toLocation") Куди (для переміщення)
            input(type="text", name="toLocation")

        button(type="submit") Виконати операцію
