nav
    ul(style="display:flex; justify-content: space-between; list-style-type:none")
        li
            a(href="/") Analytics
        if(role)
            li
                a(href="/products") Products
        if(role)
            li
                a(href="/operations") Operations
        if(role === 'admin')
            li
                a(href="/admin-dashboard") Admin Dashboard
        li
            if (email)
                a(href="/auth/logout") Logout
            else
                a(href="/auth/login") Login
        li
            if (!email)
                a(href="/auth/registration") Register
        if (email)
            li= email